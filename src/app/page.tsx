import { Icon } from "@/components";

const Page = ({ params }: { params: { slug: string } }) => {
    return (
        <div>
            <Icon name="box" />
        </div>
    );
};

export default Page;
